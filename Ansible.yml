---
#- name: first file
 # hosts: Demo
 # becomes: Yes  
 # connection: ssh
 #gather_facts: flase
 
 - hosts: demo  

  tasks:

   - name: Ansible create directory and set permissions    

     file:
       path: /backups      

        state: directory      

        mode: "u=rw,g=wx,o=rwx"    

   become: yes
